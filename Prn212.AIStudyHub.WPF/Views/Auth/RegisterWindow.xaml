<Window x:Class="Prn212.AIStudyHub.WPF.Views.Auth.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Study Hub - Đăng Ký Tài Khoản" Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" 
        Background="{StaticResource BackgroundBrush}">
    <Border Margin="24" CornerRadius="8" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" Padding="28,32">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.08"/>
        </Border.Effect>
        <StackPanel>
            <!-- Header -->
            <TextBlock Text="Tạo tài khoản mới" FontSize="22" FontWeight="Bold"
                       Foreground="{StaticResource SecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,22"/>

            <!-- Name Grid (Họ & Tên) -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Họ (*):" Style="{StaticResource FormLabelStyle}"/>
                    <TextBox x:Name="txtLastName" Style="{StaticResource ModernTextBoxStyle}" Height="38" TabIndex="1"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Tên (*):" Style="{StaticResource FormLabelStyle}"/>
                    <TextBox x:Name="txtFirstName" Style="{StaticResource ModernTextBoxStyle}" Height="38" TabIndex="2"/>
                </StackPanel>
            </Grid>

            <!-- Email -->
            <StackPanel Margin="0,0,0,12">
                <TextBlock Text="Địa chỉ Email (*):" Style="{StaticResource FormLabelStyle}"/>
                <TextBox x:Name="txtEmail" Style="{StaticResource ModernTextBoxStyle}" Height="38" TabIndex="3"/>
            </StackPanel>

            <!-- Password -->
            <StackPanel Margin="0,0,0,12">
                <TextBlock Text="Mật khẩu (*):" Style="{StaticResource FormLabelStyle}"/>
                <PasswordBox x:Name="pwdPassword" Style="{StaticResource ModernPasswordBoxStyle}" Height="38" TabIndex="4"/>
            </StackPanel>

            <!-- Confirm Password -->
            <StackPanel Margin="0,0,0,16">
                <TextBlock Text="Xác nhận mật khẩu (*):" Style="{StaticResource FormLabelStyle}"/>
                <PasswordBox x:Name="pwdConfirm" Style="{StaticResource ModernPasswordBoxStyle}" Height="38" TabIndex="5"/>
            </StackPanel>

            <!-- Error message container -->
            <TextBlock x:Name="txtError" Foreground="{StaticResource DangerBrush}" FontSize="12"
                       TextWrapping="Wrap" Margin="0,0,0,12" Visibility="Collapsed"/>

            <!-- Submit Register Button -->
            <Button x:Name="btnRegister" Content="Đăng Ký Tài Khoản" Height="42" TabIndex="6"
                    Style="{StaticResource SuccessButtonStyle}" FontSize="15" Click="BtnRegister_Click"/>

            <!-- Back link -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <TextBlock Text="Đã có tài khoản? " Foreground="{StaticResource TextMutedBrush}"/>
                <TextBlock x:Name="lnkBack" Text="Quay lại đăng nhập" Foreground="{StaticResource PrimaryBrush}"
                           FontWeight="SemiBold" Cursor="Hand" MouseLeftButtonUp="LnkBack_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
